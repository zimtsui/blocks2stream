{
  "name": "strings2stream",
  "version": "0.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b ./src/tsconfig.json"
  },
  "author": "Zim",
  "dependencies": {
    "@types/node": "^14.17.21",
    "coroutine-locks": "github:zimtsui/coroutine-locks"
  }
}
